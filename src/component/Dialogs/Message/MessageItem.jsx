import React from 'react';

export const MessageItem = (props) => {
    return(
        <div>
            {/* <spun>{props.id}</spun> */}
            <spun>{props.message}</spun>
        </div>
    )
}